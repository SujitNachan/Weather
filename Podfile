# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Ansh' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

pod 'Alamofire'
pod 'SVProgressHUD'
  # Pods for Ansh

  target 'AnshTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'AnshUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
